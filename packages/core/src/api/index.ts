// Generated API surface for web + mobile, exposed as `@monorepo/core/api`.
// Kept separate from the root barrel so the OpenAPI-codegen model names cannot
// collide with the hand-written Zod schema types (e.g. `Song`, `AuthTokens`).
//
// `./generated` and `./hooks` are produced by `pnpm --filter api generate:api`
// (see apps/api/scripts/generate-api-hooks.js) and committed to the repo.

export * from './client.js';
export * from './upload.js';
export * from './generated/index.js';
export * from './hooks/index.js';
